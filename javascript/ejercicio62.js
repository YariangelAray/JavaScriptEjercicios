// pedimos la frase
let frase = prompt("Ingrese una frase");
// damos un aviso
alert("Resultado mostrado en consola.")
// mostramos la frase 10 veces con un bucle for.
for (let i = 1; i <= 10; i++) {
    console.log(`${i}. ${frase}`);
}